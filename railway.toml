[build]
buildCommand = "apt-get install -y xvfb && pip install -r requirements.txt && playwright install --with-deps chromium"

[deploy]
startCommand = "Xvfb :99 -screen 0 1280x720x24 -ac & sleep 2 && DISPLAY=:99 python server.py"
